# Copie este arquivo para .env para testar o frontend sem o backend:
#   cp .env.mock .env
#
# Para voltar ao modo normal (com backend), apague o .env ou mude para false.
REACT_APP_MOCK_AUTH=true
REACT_APP_API_URL=http://localhost:8000/api
